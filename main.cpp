#include "normal_window.h"
#include "editor.h"

#include <iostream>
#include <fstream>
#include <ncurses.h>
#include <string>

int main() {

  Editor().Start();
/*
  while(true) {
    window.ProcessCharacter(getch());
    printw("%c", ch);
    refresh();
  }
*/

  return 0;
}
